import { HabsFilterPipe } from './habs-filter.pipe';

describe('HabsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HabsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
