import { SortBookingsPipe } from './sort-bookings.pipe';

describe('SortBookingsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBookingsPipe();
    expect(pipe).toBeTruthy();
  });
});
