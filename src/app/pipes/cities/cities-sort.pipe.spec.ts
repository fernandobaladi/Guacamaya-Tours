import { CitiesSortPipe } from './cities-sort.pipe';

describe('CitiesSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CitiesSortPipe();
    expect(pipe).toBeTruthy();
  });
});
