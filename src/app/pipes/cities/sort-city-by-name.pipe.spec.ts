import { SortCityByNamePipe } from './sort-city-by-name.pipe';

describe('SortCityByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortCityByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
