import { StatusFilterStatesPipe } from './status-filter-states.pipe';

describe('StatusFilterStatesPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusFilterStatesPipe();
    expect(pipe).toBeTruthy();
  });
});
