import { DestinationFilterPipe } from './destination-filter.pipe';

describe('DestinationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
