import { DestinationFilterByStatePipe } from './destination-filter-by-state.pipe';

describe('DestinationFilterByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationFilterByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
