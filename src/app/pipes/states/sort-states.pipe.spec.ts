import { GeneralSortPipe } from './general-sort.pipe';

describe('GeneralSortPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralSortPipe();
    expect(pipe).toBeTruthy();
  });
});
